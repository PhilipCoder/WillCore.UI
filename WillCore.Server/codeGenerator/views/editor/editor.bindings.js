/**
 * Binds the HTML elements to the collections.
 * Binding module
 * 
 * @typedef {import("./editor.meta.js").viewMetaData} view
 * @param {view} view
 */
var bindings = async (view) => {
    
};

export { bindings };