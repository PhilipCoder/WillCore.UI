/**
 * Preprocessor for view files.
 * Author : Philip Schoeman
 * */
class preProcessor {
    /**
     * Processes a view template file before saving
     * @param {any} userFileName
     * @param {any} templateName
     * @param {any} templateContent
     */
    static processFile(userFileName, templateName, templateContent) {

    }
};

module.exports = preProcessor;