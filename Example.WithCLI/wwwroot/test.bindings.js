/**
 * Binds the HTML elements to the collections.
 * Binding module
 * 
 * @typedef {import("./test.meta.js").viewMetaData} view
 * @param {view} view
 */
var bindings = async (view) => {
    
};

export { bindings };