//Auto generated JSDoc comments: 
//Used to give predictions on the view scope.

/**
*  Binds the HTML element's attributes to collection values.
*  Requires:
 *  
* */
var attributeDoc = function () {

};

/**
 *  HTML element link.
 * */
var element = function(){
/**
*  Binds the HTML element's attributes to collection values.
* */
    this.attribute= null;
/**
*  Binds HTML events.
* */
    this.event= null;
/**
*  Binds the HTML element's innerHTML to collection values.
* */
    this.innerHTML= null;
/**
*  BI directional value binding.
* */
    this.model= null;
/**
* Binds a HTML element iterator.
* */
    this.repeat= null;
/**
* Binds a partial view to an element.
* */
    this.view= null;
};


/**
 * WillCore view.
 * */
var viewMetaData = {
   
};

export { viewMetaData };