/**
 * Logic module 
 * 
 * @typedef {import("./htmlpage.meta.js").viewMetaData} view
 * @param {view} view
 * @param {object} configuration
 */
var logic = (view, configuration) => {
    return
        {
        }
};

export { logic };