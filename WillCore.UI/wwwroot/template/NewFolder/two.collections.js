/**
 * Builds up the collections that are used by the view.
 * 
 * @typedef {import("./two.meta.js").viewMetaData} view
 * @param {view} view
 * @param {object} configuration
 */
var collections = async (view, configuration) => {
    
};

export { collections };