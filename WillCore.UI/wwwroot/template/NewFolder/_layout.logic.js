/**
 * Logic module 
 * 
 * @typedef {import("./_layout.meta.js").viewMetaData} view
 * @param {view} view
 * @param {object} configuration
 */
var logic = (view, configuration) => {
    return
        {
        }
};

export { logic };